# Plan — Fase 2.3–2.5: navegação, acessibilidade e leitura de nota/devolução

Fatia: consolidar o que falta na camada de UI sobre o trabalho da Fase 2.2
(Home redesenhada), sem tocar no modelo nem no serviço.

## State atual
Home sem bottom bar (menu ⋯ para Arquivo/Configurações), arquivo
tipográfico agrupado por mês com distância temporal, NoteScreen com
FioDisplayDate, pill Guardado 1,5s, seletor temporal. Falta:

## Ações
1. [2.3] Navegação com rótulos de TalkBack: as superfícies Arquivo e
   Configurações são irmãs da Home; garantir `semantics` com role e
   labels; menu ⋯ com contentDescription; chip temporal com descrição;
   pill Guardado já usa liveRegion.
2. [2.3] Estados de erro: pill de erro terracota com ícone (ic_error novo)
   e foco de atenção discreto (sem dialog).
3. [2.5] Tela de devolução (ReturnScreen): leitura de carta — data em
   Fraunces, conteúdo, rodapé com "Reescrita com o olhar de hoje?"
   (rascunho de devolução abre o editor da Home com o texto da nota
   pré-carregado, mantendo o gesto de reescrever como parte do ritual).
   Guardado em memória (pré-modelo; a persistência exige migrations do
   PR #1 — registrar em LATER).
4. [2.5] Selo biométrico na devolução quando appLock = IMMEDIATE:
   `authorizeFresh` já existe no contrato do FioApp; devolução passa por
   ele antes de abrir (simulado com o mesmo callback).
5. Testes: build + 20 unit tests.

## Riscos
- Devolução ainda não é acionada (sem engine ativo no main antigo): a
  tela existe e pode ser explorada via "devolução simulada" do Arquivo
  (linha de teste não enviada ao usuário — deixar fora desta fatia; o
  acionamento real depende do merge do PR #1, documentado em LATER).
- Sem tests instrumentados no sandbox: acessibilidade validada apenas
  por revisão de código (gate humano documentado).

## Next
Fase 3+: engine temporal (migrations do PR #1), primeira cápsula com
persistência real, data-âncora, export v1.0, patina, ritual.
