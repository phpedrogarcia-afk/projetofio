# BOOT-PROMPT — copie e cole isto ao retomar em uma nova sessão

Você está retomando o trabalho no repositório **ProjetoFio** (`phpedrogarcia-afk/projetofio`). Antes de qualquer coisa, leia nesta ordem: `AGENTS.md`, `plans/PROJECT-STATE.md`, `plans/NEXT-WORK.md`, `plans/IDEA-INBOX.md`, `plans/CODEX-HANDOFF.md` e `MANUS-PRE-CODEX-FINAL.md`. Em resumo do que você precisa saber: o repositório passou por duas missões de engenharia concluídas (hardening com 99 testes verdes e fix criptográfico P0; red team de produto com pacote de piloto preparado), ambas em branches de integração com a `main` intacta. O trabalho seguinte depende de gates humanos do fundador (merge do PR #1, atualização do PR #3, execução de TalkBack/AVD em dispositivo, decisão sobre as 3 decisões abertas). **Não implemente nada novo** até que `NEXT-WORK.md` § THEN tenha um item desbloqueado pelo fundador. Quando implementar, siga `AGENTS.md`: princípio da menor mudança, teste reprodutor primeiro, nunca toque em `main`, nunca reporte gates humanos como aprovados. Para construir e testar: `cd app && export ANDROID_HOME=/home/ubuntu/android-sdk && ./gradlew :mobile:testDebugUnitTest --no-daemon` (a suíte completa roda em ~15 min; contar resultados nos XMLs de `mobile/build/test-results/testDebugUnitTest/`).
